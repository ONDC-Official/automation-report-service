version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: automation-report-service
    ports:
      - "3033:3000"
    env_file:
      - .env
    restart: always