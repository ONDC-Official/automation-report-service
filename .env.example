# Database service URL for fetching payloads
DATA_BASE_URL=https://dev-automation.ondc.org/db-service

# Validation service URL (use localhost for local development)
# For local validation service: http://localhost:3008/api/validate
# For production: https://log-validation.ondc.org/api/validate
VALIDATION_URL=http://localhost:3008/api/validate

# Server port
PORT=3000

# Utility mode flag
UTILITY=false

# Redis configuration for caching
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password
REDIS_USERNAME=your_redis_username

# API service authentication key
API_SERVICE_KEY=your-api-key

# Base URL for retail API service (used for constructing BAP/BPP IDs)
RETAIL_API_SERVICE_BASE_URL=https://dev-automation.ondc.org/api-service